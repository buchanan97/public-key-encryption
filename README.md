# public-key-encyhrption
two Python scripts attached following the instructions in the comments. The scripts implement the two parties of a Diffie-Hellman key exchange, and communicate over the local network interface. The first party (alice.py) connects to the second party (bob.py).
